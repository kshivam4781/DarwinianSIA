.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
runs/
*.egg-info/
dist/
build/
.cursor/*
!.cursor/environment.json
*.log
# Local GPQA diamond CSV drops (Tick 277) — never commit gated dataset exports
docs/private/
.local/gpqa_diamond.csv
/tmp/gpqa_diamond.csv
gpqa_diamond.csv
# Tick 285: docs/icml_budget_spent.json is NOT gitignored — amounts are not
# secrets; committing the ledger is required for cross-VM cron resume
# (runs/ stay gitignored and do not survive fresh cloud boots).
# Tick 356: per-VM greenfield boot branch for open_git_pr warn — must NOT be
# committed (would poison tip with a stale boot name) and must survive
# discard_ephemeral_icml_dirt / tip --apply (gitignored → invisible to porcelain).
docs/icml_cloud_boot_branch.txt
# Tick 359: open_git_pr MCP call JSON carries cloud_boot_branch — must NOT be
# committed (tip HEAD had Tick-357 `…-48b0`; discard_ephemeral restore re-poisoned
# fresh boots) and must survive tip --apply (gitignored + not in EPHEMERAL).
docs/icml_open_git_pr_call.json
